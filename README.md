# Klopen
 **Klopen** is an open-source, rule-based Python framework for reproducible, sentence-level text extraction and corpus analysis from PDF documents (for example annual and sustainability reports). It converts unstructured PDFs into an auditable, deterministic sentence-level corpus suitable for downstream content analysis and LLM-assisted annotation.
